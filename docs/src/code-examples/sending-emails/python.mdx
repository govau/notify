---
lang: python
---


```python
notifications_client.send_email_notification(
    email_address='lisa@example.com',
    template_id='f33517ff-2a88-4f6e-b855-xxxxxxxxxxxx',
    personalisation={
        'user_name': 'Lisa',
        'amount_owing': '$15.50'
    }
)
```

<Panel label={<H3>Arguments</H3>}>
<Parameters>
  <Parameter required name="template_id">

The ID of the template

You can find this by signing into Notify and
going to the Templates page.

  </Parameter>
  <Parameter required name="email_address">

The email address of the recipient you want to send to.

  </Parameter>
  <Parameter optional name="personalisation">

If a template has placeholder fields for personalised information such as name
or reference number, you must provide their values in a map with key
and value pairs.

These pairs should be structured such that they key is the name
of the value in your template, and the value is what you expect to be
substituted in the message.

For example,

```json inline
{
    "first_name": "Lisa",
    "amount_owing": "$15.50"
}
```

  </Parameter>
  <Parameter optional name="reference">

A unique identifier you create. This reference identifies a single unique notification or a batch of notifications. If you do not have a reference, you must pass in an empty string or null.

  </Parameter>
  <Parameter optional name="email_reply_to_id">

An optional identifier that you can get from the service email_reply_to ids found in the service settings / manage email reply to addresses page.

You can omit this argument if you want to use the default service email reply to otherwise add the id from the list of email_reply_to ids associated with the service.

  </Parameter>

</Parameters>
</Panel>

<Panel label={<H3>Response</H3>}>

If successful, the shape of the response you recieve will resemble the
following.


```json
{
  "id": "740e5834-3a29-46b4-9a6f-16142fde533a",
  "reference": None,
  "content": {
    "subject": "Licence renewal",
    "body": "Dear Bill, your licence is due for renewal on 3 January 2016.",
    "from_email": "the_service@notify.gov.au"
  },
  "uri": "https://rest-api.notify.gov.au/v2/notifications/740e5834-3a29-46b4-9a6f-16142fde533a",
  "template": {
    "id": "f33517ff-2a88-4f6e-b855-c550268ce08a",
    "version": 1,
    "uri": "https://rest-api.notify.gov.au/v2/template/f33517ff-2a88-4f6e-b855-c550268ce08a"
  }
}
```

</Panel>
