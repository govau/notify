---
lang: node
---

```javascript
client.sendSms(templateId, phoneNumber, {
  personalisation: personalisation,
  reference: reference,
  smsSenderId: smsSenderId,
})
```

<Panel label={<H3>Arguments</H3>}>

#### Ordered arguments

<Parameters>
  <Parameter required name="templateId">

The ID of the template

You can find this by signing into Notify and
going to the Templates page.

  </Parameter>
  <Parameter required name="phoneNumber">

The phone number of the recipient, required for SMS notifications.

  </Parameter>
</Parameters>

#### Named arguments

<Parameters>
  <Parameter optional name="reference">

A unique identifier you create. This reference identifies a single unique notification or a batch of notifications. If you do not have a reference, you must pass in an empty string or null.

  </Parameter>
  <Parameter optional name="smsSenderId">

Specifies the identifier of the SMS sender to set for the
notification. The identifiers are found in your service Settings, when you
'Manage' your 'Text message sender'.

If you omit this argument your default SMS sender will be set for the
notification.

  </Parameter>
  <Parameter optional name="personalisation">

If a template has placeholder fields for personalised information such as name
or reference number, you must provide their values in an object.

The object should be structured such that they key is the name
of the value in your template, and the value is what you expect to be
substituted in the message.

For example:

```json inline
(personalisation = {
  "first_name": "Amala",
  "reference_number": "300241"
})
```

  </Parameter>
</Parameters>
</Panel>

<Panel label={<H3>Response</H3>}>

If the request is successful, the response will be an object.

```json
{
  "id": "bfb50d92-100d-4b8b-b559-14fa3b091cda",
  "reference": null,
  "content": {
    "body": "Some words",
    "from_number": "40604"
  },
  "uri": "https://rest-api.notify.gov.au/v2/notifications/ceb50d92-100d-4b8b-b559-14fa3b091cd",
  "template": {
    "id": "ceb50d92-100d-4b8b-b559-14fa3b091cda",
    "version": 1,
    "uri": "https://rest-api.notify.gov.au/v2/templates/bfb50d92-100d-4b8b-b559-14fa3b091cda"
  }
}
```

</Panel>
