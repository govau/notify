---
defaults: &defaults
  buildpack: nodejs_buildpack
  instances: 1
  memory: 1G
  command: make run
  env:
    NODE_ENV: development

applications:
- name: notify-graphql-api-((stg))
  <<: *defaults
