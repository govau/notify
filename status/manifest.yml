defaults: &defaults
  buildpack: ruby_buildpack
  instances: 1
  memory: 1G
  services:
    - notify-mysql-status
  env:
    RUNNER: ""
    PRODUCTION: true

applications:
  - name: notify-status
    <<: *defaults
    routes:
      - route: notify-status.apps.y.cld.gov.au
